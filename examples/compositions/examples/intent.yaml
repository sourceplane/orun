apiVersion: sourceplane.io/v1
kind: Intent
metadata:
  name: example-platform
  description: Example intent showing how to consume the example-platform-compositions stack.
compositions:
  sources:
    - name: example-platform
      kind: oci
      ref: ghcr.io/sourceplane/example-platform-compositions:latest
discovery:
  roots:
    - apps/
environments:
  development:
    defaults:
      lane: dry-run
