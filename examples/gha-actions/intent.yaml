apiVersion: sourceplane.io/v1
kind: Intent

metadata:
  name: gha-actions-demo
  description: Minimal example that exercises GitHub Actions use steps during arx run

discovery:
  roots:
    - services/

environments:
  development:
    defaults:
      logLevel: debug
