# Docs last updated for version

v2.17.0

## How to use this file

When updating docs next time, run:

```bash
# Get all changes since docs were last updated
git log v2.17.0..HEAD --oneline
```

Then update this file to the new version after the docs PR is merged.

## What was covered in this update

- Orun Cloud client (v2.17.0 epic, OC0–OC6): the remote state backend graduated
  to the Orun Platform client
  - Wired the existing (previously orphaned) CLI references into the sidebar:
    cli/orun-auth.md (CLI session login/status/token) and cli/orun-cloud.md
    (repo ↔ org/project link + scope)
  - Updated: cli/orun-catalog.md — new `push` subcommand (sync the snapshot +
    advance the catalog head) and `refresh --push`; see-also to cloud/auth
  - Updated: cli/orun-run.md — `--local` escape hatch (force local state when the
    backend is down)
  - Updated: cli/orun-logs.md — `--follow` live tail (fromSeq cursor)
- Cockpit Catalog surface (covered in the release notes): Catalog replaces the
  Components surface; two-line header with source context + animated brand mark;
  stabilized live step-log view
- Release notes: release-notes/v2.17.0.md; sidebars.js updated; docusaurus
  navbar "Releases" link bumped to v2.17.0
- README.md: install/version references bumped v2.16.0 → v2.17.0

## Deferred / not yet documented

- Work plane (orun-work, W0–W4) is internal-only (system of record, PR-webhook
  ingestion, delivery-decision core, content-addressed sealing). No user-facing
  CLI surface yet, so no CLI reference page — mentioned in the release notes only.
- objgolden cross-language golden vectors (developer/contributor tooling).
</content>
