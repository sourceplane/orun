# Task 0002

Agent: Implementer

## Current Repo Context

- Milestone M0 (Foundation) merged via PR #152 → main commit `4ea1980e`.
  `github.com/oklog/ulid/v2 v2.1.1` is a direct require; `internal/testfx/statefs`
  exposes `NewWorkspace`, `AssertJSONFile`, and `ReadJSON[T]`; `make
  test-state-redesign` exists (no-op).
- Active spec: `specs/orun-state-redesign/` (Phase 1, local-only).
- Active milestone: **M1 — `internal/triggerctx`** per
  `specs/orun-state-redesign/implementation-plan.md`. M1 is the trigger context
  model that every downstream milestone (statestore, revision, executionstate,
  CLI) consumes.
- `internal/trigger/` already exists (match.go, normalize.go, resolve.go,
  validate.go). `internal/triggerctx/` does NOT yet exist.
- Carry-over chores from Task 0001's verifier report:
  1. Delete `internal/testfx/statefs/tools.go` once the first real production
     import of `github.com/oklog/ulid/v2` lands, and confirm `go mod tidy`
     keeps the require in the **direct** block of `go.mod`.
  2. The spec text in `specs/orun-state-redesign/test-plan.md §1`/§3 references
     `github.com/flyingmutant/rapid`, but `go.mod` pins
     `pgregory.net/rapid v1.1.0` (same library, current canonical import path).
     Use the path that compiles; file a spec-clarification proposal as part of
     this task.

## Objective

Introduce the `internal/triggerctx` package implementing M1 of the active spec:
the trigger context model + resolver that future milestones (statestore,
revision, executionstate, CLI rewire) consume. No persistence is added in this
task — model and resolver only.

## PR Boundary

You have latitude to ship M1 as either one PR or a clean two-PR split per the
spec's suggested scope (types+ids+system constructors first; then declared wrap
+ dispatcher + tests). Constraints:

- **In scope (this milestone):**
  - New package `internal/triggerctx/` with: `context.go`, `ids.go`, `system.go`,
    `declared.go`, `resolve.go` (plus matching `_test.go` files).
  - First real production import of `github.com/oklog/ulid/v2` (the ULID
    generator in `ids.go`). Remove `internal/testfx/statefs/tools.go`; run
    `go mod tidy`; verify the ulid require remains in `go.mod`'s direct block.
  - One spec-clarification proposal at
    `ai/proposals/task-0002-spec-update.md` correcting the rapid import path
    in `specs/orun-state-redesign/test-plan.md` (file the proposal; the spec
    edit itself is a clarification and may be folded into this PR per the
    Spec Change Proposals rules in `agents/orchestrator.md`).
  - Tests: property test for `TriggerKey` stability + format; table-driven
    tests for all four `ResolveTriggerContext` branches; tests for system
    constructors and `FromDeclaredTrigger` / `ResolveProviderEvent`.
- **Out of scope (non-goals, see below):**
  - No `StateStore` interface (M2), no `.orun/revisions/` writes, no manifests,
    no executions, no CLI surface changes, no persistence.

## Read First

- `specs/orun-state-redesign/README.md` — index + read order.
- `specs/orun-state-redesign/implementation-plan.md` — Milestone M1 entry
  (goal, files, dependencies, suggested PR scope, "done when").
- `specs/orun-state-redesign/data-model.md` §2 (`TriggerOccurrence`, including
  schema for `TriggerSource` and `PlanScope`).
- `specs/orun-state-redesign/design.md` §5.1 (architecture / writer order for
  the dispatcher's role) and §11 (error taxonomy — note the typed error shape
  for `--from-ci` no-match).
- `specs/orun-state-redesign/test-plan.md` §1 (coverage targets), §3 (property
  tests — note the rapid import-path clarification this task files).
- `internal/trigger/{match.go,resolve.go,normalize.go,validate.go}` — the
  existing trigger machinery `declared.go` wraps.
- `internal/testfx/statefs/` — confirm leaf-clean status; this task removes
  `tools.go` once a real `ulid` import lands in production code.

## Required Outcomes

- [ ] `internal/triggerctx/context.go` defines `TriggerOccurrence`,
      `TriggerSource`, and `PlanScope` exactly per `data-model.md §2`.
- [ ] `internal/triggerctx/ids.go` implements a monotonic ULID generator
      prefixed `trg_`, plus `TriggerKey()`, `normalizeScope()`, `shortSHA()`,
      and `worktreeMarker()` helpers. Property test asserts stability + format
      of `TriggerKey`.
- [ ] `internal/triggerctx/system.go` implements `NewSystemManual`,
      `NewSystemManualChanged`, `NewSystemReplay`, `NewSystemAPI`,
      `NewSystemMigrated`.
- [ ] `internal/triggerctx/declared.go` wraps `internal/trigger`:
      `FromDeclaredTrigger(...)` and `ResolveProviderEvent(...)` (the latter
      backing `--from-ci`).
- [ ] `internal/triggerctx/resolve.go` exposes
      `ResolveTriggerContext(opts, intent, git)` dispatching to system /
      declared / provider-event / replay branches; `--from-ci` no-match returns
      the typed error from `design.md §11`.
- [ ] `internal/testfx/statefs/tools.go` deleted; `go mod tidy` keeps
      `github.com/oklog/ulid/v2 v2.1.1` in the **direct** require block of
      `go.mod`.
- [ ] `ai/proposals/task-0002-spec-update.md` exists, naming the
      `flyingmutant/rapid` → `pgregory.net/rapid` clarification.
- [ ] All new exported symbols documented (godoc comments).
- [ ] Branch pushed, PR opened, implementer report filed.

## Non-Goals

- No `StateStore` interface, paths, or local driver (M2).
- No `internal/revision`, `internal/executionstate`, manifests, or refs.
- No CLI rewire (M5).
- No changes outside `internal/triggerctx/`, `internal/testfx/statefs/tools.go`
  deletion, the spec-clarification edit/proposal, and `go.mod`/`go.sum`.
- No new public consumers of `triggerctx` in this PR (the package is wired in
  M2+).
- No TUI cockpit work; no Phase 2/3 (R2/S3/Supabase/DO) changes.

## Constraints

1. Keep `internal/testfx/statefs` leaf-clean — do NOT import it from
   `internal/triggerctx`. If you need fixtures, put them inside
   `internal/triggerctx` itself or use a dedicated sibling under
   `internal/testfx/`.
2. Use `pgregory.net/rapid` (already pinned at `v1.1.0`) for the property
   test. Do NOT add `github.com/flyingmutant/rapid`.
3. ULID generation must be monotonic across a single process (sort-stable
   within the same millisecond). Seed `oklog/ulid/v2` accordingly.
4. `TriggerOccurrence` JSON must match `data-model.md §2` field names exactly
   (`apiVersion` = `"orun.io/v1alpha1"`, `kind` = `"TriggerOccurrence"`, etc.).
5. The package must not import any other Orun production package besides
   `internal/trigger` (and stdlib + `github.com/oklog/ulid/v2` +
   `pgregory.net/rapid` in tests).
6. If you discover behavior, contract, or persistence changes required beyond
   the spec text, stop and write `/ai/proposals/task-0002-<topic>.md`. Do not
   silently widen scope.

## Integration Notes

- M2 (`internal/statestore`) will import `TriggerOccurrence` / `TriggerKey`
  shortly — design the types so they are JSON-stable and copy-safe.
- M3 (`internal/revision`) needs `TriggerKey(trig, planHash)` to be stable
  given the same logical inputs; this is the property test's contract.
- The `--from-ci` no-match typed error in `design.md §11` will be used by the
  CLI in M5 to produce a deterministic exit code. Surface it as a sentinel or
  typed error (whichever the spec wording supports) and document the choice.

## Acceptance Criteria

✅ `go build ./...` exits 0.
✅ `go vet ./...` exits 0.
✅ `go test ./...` exits 0.
✅ `go test -count=1 -cover ./internal/triggerctx/...` reports ≥ 90 %
    statement coverage.
✅ Property test for `TriggerKey` (rapid) passes ≥ 100 iterations on a clean
    seed.
✅ `make test-state-redesign` runs and passes (extend the target to invoke the
    new package's tests if not already wildcarded).
✅ `go list -m github.com/oklog/ulid/v2` returns `v2.1.1` AND the require is
    in the **direct** block of `go.mod` after `go mod tidy`.
✅ `internal/testfx/statefs/tools.go` no longer exists.
✅ `ai/proposals/task-0002-spec-update.md` exists with the rapid-path
    clarification.
✅ Branch pushed, GitHub PR opened, CI green or progressing.

## Verification

Local commands the Implementer must run (and record in the report):

```bash
go mod tidy
go build ./...
go vet ./...
go test -count=1 -cover ./internal/triggerctx/...
go test ./...
make test-state-redesign
go list -m github.com/oklog/ulid/v2
/Users/irinelinson/.local/bin/kiox -- orun validate --intent intent.yaml
/Users/irinelinson/.local/bin/kiox -- orun plan --changed --intent intent.yaml --output plan.json   # may fail locally on the known composition-cache quirk; record it
/Users/irinelinson/.local/bin/kiox -- orun run --plan plan.json --dry-run --runner github-actions   # no-op plan acceptable
gh pr view <PR#> --json title,state,mergeable,statusCheckRollup
```

## PR Creation Requirement

You must create a task branch (`impl/task-0002-m1-triggerctx` recommended, but
you may reuse a branch if you split into 2 PRs), commit the scoped changes,
push, and open a GitHub PR. Reporting `PR Number: TBD` is not acceptable. If
PR creation is impossible, file the implementer report with `PR Number: BLOCKED`
and record the exact command + error.

After creating the PR, update `ai/reports/task-0002-implementer.md` (and any
context files still carrying placeholder `#[PR]` / `TBD`) with the real PR
number(s), then commit and push that final update before declaring the task
complete.

## When Done Report

Write `/ai/reports/task-0002-implementer.md` with these sections (concise):

- Summary (3–5 bullets)
- Files Changed (grouped by subsystem)
- Checks Run (exact commands + result)
- Assumptions (durable only)
- Spec Proposals (link to `ai/proposals/task-0002-spec-update.md` + any
  others, one-line reason each)
- Remaining Gaps (actionable residual risk only — e.g. tools.go removal,
  rapid path clarification status)
- Next Task Dependencies (what M2 needs from this PR)
- PR Number (real number, or `BLOCKED` with cause)
