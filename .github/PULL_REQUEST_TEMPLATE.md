## What changed

<!-- One or two sentences from the user's point of view. Link the issue if there is one. -->

## Why

<!-- The problem this solves, or the behaviour it corrects. -->

## How it was verified

<!-- Commands you ran, tests you added, and what you observed. -->

## Checklist

- [ ] `make fmt lint test` passes locally
- [ ] Tests cover the change (a regression test for a bug fix)
- [ ] Documentation under `website/docs/` is updated for any user-visible change
- [ ] Release notes updated under `website/docs/release-notes/` if user visible
- [ ] Commits are signed off (`git commit -s`)
