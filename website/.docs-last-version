# Docs last updated for version

v1.25.0

## How to use this file

When updating docs next time, run:

```bash
# Get all changes since docs were last updated
git log v1.25.0..HEAD --oneline
```

Then update this file to the new version after the docs PR is merged.

## What was covered in this update

- Multi-kind composition authoring (ComponentSchema, JobTemplate, ExecutionProfile)
- Split-kind directory structure with `composition.yaml`, `schema.yaml`, `jobs/`, `profiles/`
- Capability-based step selection via `includeCapabilities`
- Profile step overrides via `stepOverrides`
- Profile policies (requireCleanGitTree, requireApproval, etc.)
- All 11 example compositions converted to split-kind format
- Updated: composition-contract.md, composition-examples.md, writing-compositions.md
- Updated: concepts/compositions.md, concepts/stacks.md
