# Task 0008 — Finalize Task 0007 (commit, push, PR open) — M3 PR-A delivery chore

## Agent
Implementer

## Current Repo Context

Task 0007 (M3 PR-A — `internal/revision` model + keys + writer skeleton) was
**implemented locally but never delivered**:

- Branch `impl/task-0007-m3-revision-pra` exists locally; **no commits**.
- All 11 changed files are **staged** (`git status` shows `Changes to be
  committed`): `Makefile` (modified) + `internal/revision/{model,keys,writer,version,keys_test,writer_test,coverage_test}.go` (new) + `ai/tasks/task-0006-verifier.md`, `ai/tasks/task-0007.md`, `ai/reports/task-0007-implementer.md` (new docs).
- **No PR.** `gh pr list --head impl/task-0007-m3-revision-pra` returns `[]`.
- The branch is **not pushed** to `origin`.
- `ai/reports/task-0007-implementer.md` is filed but **lacks a `PR Number`
  line**; it claims local quality gates green (revision pkg coverage 93.3 %,
  statestore 96.1 %, `go test -race`, `make test-state-redesign`, leaf-clean).

Per `agents/orchestrator.md` Implementer Standard: *"if a PR cannot be created,
the report must mark the task blocked instead of complete… `PR Number` must be
the created GitHub PR number or an explicit `BLOCKED` entry. `TBD` is not an
acceptable completed implementer report value."* Task 0007 therefore cannot
advance to verification until a PR exists.

This task is a **delivery chore, not a code task.** Its purpose is to convert
the locally-staged Task 0007 work into a real reviewable PR so the next cycle
can emit the M3 PR-A verifier.

## Objective

Commit the staged Task 0007 tree on branch `impl/task-0007-m3-revision-pra`,
push it to `origin`, open a GitHub PR titled `Task 0007: M3 PR-A — internal/revision model + keys + writer skeleton`,
backfill the real PR number into `ai/reports/task-0007-implementer.md`, and
file a short Task 0008 implementer report describing the delivery chore.

## PR Boundary

**Same PR as Task 0007.** This task does not open a separate PR — it ships the
Task 0007 PR. One commit (or two: code + report-update) on
`impl/task-0007-m3-revision-pra`, pushed, opened as a PR.

In scope:

1. Commit the currently-staged Task 0007 tree (Makefile + `internal/revision/**`
   + `ai/tasks/task-0006-verifier.md` + `ai/tasks/task-0007.md` +
   `ai/reports/task-0007-implementer.md`).
2. Push the branch.
3. Open the PR via `gh pr create` and capture the assigned number.
4. Update `ai/reports/task-0007-implementer.md` to add a `## PR Number`
   section with the real number; commit + push that follow-up edit.
5. File `ai/reports/task-0008-implementer.md` documenting the chore.

Out of scope:

- **No code changes** to `internal/revision/**`, `Makefile`, or any other
  production / test file. If `go build ./...` or `go test ./...` fails on the
  current staged tree, STOP and re-stage Task 0007 with the fix; do not silently
  amend the M3 PR-A scope from inside this chore.
- **No spec edits.** Concerns about claim-first ordering vs `cli-surface.md`
  §1.2 step-7 (raised in `ai/reports/task-0007-implementer.md` § "Step-Order
  Deviation") are deferred to the verifier; if a proposal is needed, the
  verifier files it under `ai/proposals/task-0007-spec-update.md`.
- No production-caller wiring.

## Read First

- `agents/orchestrator.md` — Implementer Standard, PR Creation Requirement.
- `ai/tasks/task-0007.md` — original M3 PR-A scope (for the PR description body).
- `ai/reports/task-0007-implementer.md` — current implementer summary; this
  task only edits it to add `## PR Number`, nothing else.
- `specs/orun-state-redesign/implementation-plan.md` — Milestone M3 "Done when".

## Required Outcomes

- [ ] `git log --oneline impl/task-0007-m3-revision-pra ^origin/main` shows ≥ 1
      commit covering the Task 0007 tree.
- [ ] `git ls-remote origin impl/task-0007-m3-revision-pra` returns a SHA
      (branch is pushed).
- [ ] `gh pr list --head impl/task-0007-m3-revision-pra --json number,state`
      returns a single OPEN PR; capture its number as `<PR>`.
- [ ] PR title: `Task 0007: M3 PR-A — internal/revision model + keys + writer skeleton`.
- [ ] PR body links Task 0007 prompt and summarises files changed (Implementer
      may copy from `ai/reports/task-0007-implementer.md` § Summary).
- [ ] `ai/reports/task-0007-implementer.md` ends with a populated
      `## PR Number` section: a single line `#<PR>`. Update committed and
      pushed to the same branch.
- [ ] `ai/reports/task-0008-implementer.md` exists with sections per the
      orchestrator report budget (Summary, Files Changed, Checks Run,
      Assumptions, Spec Proposals, Remaining Gaps, Next Task Dependencies,
      PR Number) and `PR Number: #<PR>` (same number as Task 0007 — Task 0008
      ships into the same PR as a follow-up commit).
- [ ] Quality gates rerun on the committed tree before push:
      `go build ./...`, `go vet ./...`,
      `go test -race -count=1 ./internal/revision/... ./internal/statestore/... ./internal/triggerctx/...`,
      `make test-state-redesign`. All green.
- [ ] PR CI shows `CI / Orun Plan` and `Harness dry-run guard` SUCCESS (or
      noted in-progress with run IDs) before this task is reported complete.

## Non-Goals

- No additional revision-package code, no test additions/removals, no
  refactor of the staged tree.
- No spec edits or proposals. M3 PR-A correctness review is the verifier's job.
- No `cmd/orun` / `internal/state` / `internal/runner` / `internal/runbundle`
  changes.
- No M3 PR-B work (manifest writer, `ResolveRevision`, legacy `.orun/plans/**`
  mirror body).

## Constraints

1. **Single-branch single-PR:** `impl/task-0007-m3-revision-pra` only.
2. **No amend of staged scope.** If a quality gate fails, re-stage the smallest
   targeted fix (commit it as `fix: …` on the same branch) and re-run gates;
   do not delete or rewrite the M3 PR-A surface.
3. **Real PR number, not `TBD`.** `ai/reports/task-0007-implementer.md` and
   `ai/reports/task-0008-implementer.md` MUST land with the assigned number.
4. **CI is authoritative for `orun plan --changed`.** The persistent local
   composition-cache quirk (stack.yaml at `~/.orun/cache/compositions/c41fc08…
   has no spec.compositions`) reproduces sporadically; if `kiox -- orun plan
   --changed --intent intent.yaml --output plan.json` fails locally, document
   it and rely on PR CI (Task 0001-0007 pattern).

## Integration Notes

The PR description body must:
- Cite `agents/orchestrator.md` §"Implementer Standard" and Task 0007 as the
  source of truth for scope.
- List the seven-step ordered write list from `cli-surface.md` §1.2 / `design.md`
  §5.1 and call out the **claim-first deviation** the implementer made (index
  reservation before body writes), pointing the verifier at
  `ai/reports/task-0007-implementer.md` § "Step-Order Deviation From cli-surface.md §1.2".
- Note that legacy `.orun/plans/<checksum>.json` mirroring is a `// TODO(m5)`
  stub gated by `Config.CompatibilityWrites` (default `true`, unwired body).
- Note that `ResolveRevision` and `WriteManifest` are deferred to PR-B (Task
  0009 once verifier passes M3 PR-A).

## Acceptance Criteria

✅ Branch pushed; PR open and MERGEABLE.
✅ PR has the documented title and body.
✅ Both implementer reports (0007 and 0008) carry `PR Number: #<PR>`.
✅ Local quality gates green on the final committed tree.
✅ Required CI checks (`CI / Orun Plan`, `Harness dry-run guard`) at minimum
   queued/in-progress, ideally SUCCESS, before report close.

## Verification

Verifier task (Task 0009) will validate Task 0007's M3 PR-A scope against
`specs/orun-state-redesign/implementation-plan.md` Milestone M3 "Done when",
review the claim-first deviation, run `make test-state-redesign`, inspect
PR CI logs, and merge per the Verifier Merge Protocol. Task 0008's chore
itself is verified implicitly (the PR exists).

## PR Creation Requirement

You must commit, push, and open the GitHub PR before reporting complete.
`PR Number: TBD` is not acceptable. After `gh pr create` returns the number,
update `ai/reports/task-0007-implementer.md` with `## PR Number\n#<PR>\n`,
commit + push, then write `ai/reports/task-0008-implementer.md` with the same
PR number.

## When Done Report

`/ai/reports/task-0008-implementer.md` with:

- Summary (3-5 bullets — what was committed, when, where pushed, PR opened, reports updated)
- Files Changed (from this task only — should be just two doc edits: the 0007 PR-Number patch + the new 0008 report; everything else is the carried-over Task 0007 staged tree)
- Checks Run (build, vet, -race tests, make test-state-redesign; CI run IDs once available)
- Assumptions (durable only; e.g. claim-first deviation acknowledged but deferred to verifier)
- Spec Proposals (none expected — defer to verifier)
- Remaining Gaps (M3 PR-B = ResolveRevision + manifest + legacy mirror body)
- Next Task Dependencies (Task 0009 = M3 PR-A verifier; Task 0010 = M3 PR-B implementer)
- PR Number (same number as Task 0007 — single-PR delivery)
