# orun-agents — Implementation Status (as-built)

The as-built record, kept distinct from the design/plan docs. One row per
milestone; the `As-built` cell names exactly what landed.

| Milestone | Status | As-built |
|-----------|--------|----------|
| AG0 — Object kinds + foundation | ✅ Shipped | `internal/nodes/agents.go`: `AgentTypeSnapshot` (tree: `agent-type.json` + `body.md` [+ `base-literacy.md`], doc-blob spine, mandatory owner), `AgentBrief`, `AgentSessionSegment` (prev-chained, closed 11-kind event vocabulary — no status kind exists), `AgentSessionSnapshot` (outcome carries no timestamps/cost — annotation stays out of identity); kinds registered in `kinds.go`; pure ids (`AgentTypeID`/`AgentBriefID`/`AgentSessionSegmentID`/`AgentSessionID`) in `ids.go` via the `hashStore` cross-check. `internal/agent`: package skeleton + embedded versioned **base literacy** (`literacy.md`, `LiteracyName`/`LiteracyVersion`, `SealLiteracy` idempotent under `refs/agents/literacy/v1`). CLI: `orun agent context [--seal]` + `orun agent context id` (`cmd/orun/command_agent.go`). Tests: determinism (map-order/whitespace invariance, persona/model edits change identity), pure-id ↔ assembled-id parity, validation rejections (ownerless, empty persona, unknown event kind incl. `status_asserted`, non-monotonic seq), literacy seal idempotency. |
| AG1 — Authoring + seal/pull + catalog projection | ✅ Shipped | `internal/agenttype`: closed-schema frontmatter loader (`KnownFields`, kind-gated skip for non-agent files, duplicate detection, node-schema validation — one rulebook). Resolver: `ResolvedCatalog.AgentTypes` scanned from `agents/*.md` in `resolve_full.go` (load errors demote to `agenttype.invalid` warnings, never abort). Projection: `EntityKindAgentType` + `agentTypeEntity` in `internal/objplan/catalog.go` — persona as pending doc blob (digest-stamped), `mayAffect` globs kept in spec **and** resolved to typed `mayAffect` edges against component keys (`matchComponentGlob`: full-key + last-segment `path.Match`); `objcatalog.readEntities` projects with no reader change. Seal: `nodewriter.WriteAgentType`; CLI `orun agent lint/import/show` (import pins the binary literacy via `extends@<id>`, ref `agents/types/<name>/latest`; show materializes from content alone). Dogfood: `agents/orchestrator.md` converted (frontmatter added), `agents/implementer.md` authored; both seal idempotently. Networked pull deferred to the WP remote slice (mirrors `orun spec pull`'s v1 posture). Tests: agenttype loader suite (rejections, key-order identity), objplan projection + full catalog-tree assembly with persona blob round-trip. |
| AG2 — The delegation runtime | ✅ Shipped | `internal/agent/driver`: the `Driver` seam (Launch → normalized `Event` stream: message/tool_call/tool_result/approval/artifact/cost/error/done), a registry, and an in-process deterministic `Stub` driver (runs the whole loop with no binary/model/network). `internal/agent`: `AssembleBrief` (renders literacy+persona+contract, seals the instructions + a frozen `AffectedSet` + the `AgentBrief` node — deterministic by content), `ToolPolicy` (deny-by-default; exact beats wildcard, deny>ask>allow), `SessionLog` (append-only, closed vocabulary, prev-chained `AgentSessionSegment` sealing), and `Run` (the loop: launch → fold events into the log with policy enforcement → outcome; ask-gated tools consult an approver, auto-deny when none). CLI: `orun agent run [--type --task --spec --driver --dry-run --json]` + `orun agent drivers` (`cmd/orun/command_agent_run.go`); `--dry-run` seals+prints the brief. Tests: brief determinism, full stub-loop → sealed session with transcript replay (artifact+tool_call present, no forbidden kinds), denied+ask tool handling, tool-policy precedence. Smoke-verified end-to-end (dry-run brief + live stub session → PR outcome). Claude Code driver + live MCP wiring ride AG4/WP5. |
| AG3 — TUI Agent mode | ✅ Shipped | `internal/tui`: `ModeAgent` registered as the 6th cockpit mode (key `3`, String/dispatch/keymap/switchMode wired); `views/agent.go` `AgentModel` — a two-pane surface (agent types list ↔ detail: harness/model/owner/autonomy/mayAffect/persona) with a **streaming transcript pane** (`AgentTranscriptEvent` + `WaitForAgentEvent`, the established run-view stream pattern; nil until a run attaches). `internal/tui/services.LoadAgentTypes` reads the AG1 `AgentType` catalog entities (persona resolved from its content blob; `mayAffect` surfaced on entity metadata so the read view projects it) — added to the `OrunService` interface + mock. `internal/agent`: a nil-safe `Observe` hook on `RunOptions`/`SessionLog` (the live-visibility seam the TUI + cloud DO relay consume). Tests: view render/empty-state/cursor/filter/live-stream, service projection from a real seeded catalog, and model-level mode switch + load + render. In-TUI launch trigger (store-backed run service) pairs with AG4's session sealing; today a run streams via `orun agent run` and the surface renders types + any attached transcript. |
| AG4 — Driver conformance + session seal | ✅ Shipped | `internal/agent/driver`: `CheckConformance` — the reusable oracle documenting + checking the driver protocol (non-blocking Launch, normalized events, terminal Done/Wait, deadline-bounded); a second `echoDriver` passes it unchanged and a protocol-violating `badDriver` is caught (the oracle means something). `internal/agent`: `SealSession` (AgentSessionSnapshot pinning agentType/brief/catalog/segments/principal + outcome; ref `refs/agents/sessions/<id>`; idempotent) and `Replay` (folds the sealed segment chain to a transcript from content alone). `Run` seals on terminal state when `RunOptions.Seal`+`Refs` are set (returns `SnapshotID`). CLI: `orun agent run` now seals + prints the session id/replay hint; `orun agent replay <session>[@id]` re-renders the transcript. Tests: conformance (stub + second driver + violation catch + registry), seal idempotency, run→seal→replay round-trip, and the provenance walk (session → brief → instructions, session → agent type — all by content hash). Smoke-verified: import → run --type → sealed session ref → replay reproduces the 8-event transcript. |
