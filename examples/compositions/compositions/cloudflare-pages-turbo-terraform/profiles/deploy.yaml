apiVersion: sourceplane.io/v1
kind: ExecutionProfile
metadata:
  name: deploy
spec:
  description: Build app and apply Terraform to reconcile Cloudflare Pages project
  plan:
    scope: full
  controls:
    cloudflare-pages-turbo-terraform:
      build:
        enabled: true
      terraform:
        plan: true
        apply: true
