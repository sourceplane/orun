# Docs last updated for version

v2.1.0

## How to use this file

When updating docs next time, run:

```bash
# Get all changes since docs were last updated
git log v2.1.0..HEAD --oneline
```

Then update this file to the new version after the docs PR is merged.

## What was covered in this update

- Profile rules: conditional profile selection via `profileRules` on subscriptions
- Template context: namespaced `.orun`, `.parameters`, `.env` (breaking: flat context removed)
- Parameters model: `inputs`→`parameters`, `defaults`→`parameterDefaults` (breaking)
- `PlanJob.config` removed (breaking)
- ORUN_ENV as portable alias for GITHUB_ENV
- Intent presets: stack-published reusable intent scaffolding
- Environment promotion dependencies
- Updated: concepts/profile-rules.md (new)
- Updated: concepts/intent-model.md (profileRules reference)
- Updated: reference/configuration.md (profileRules syntax)
- Updated: compositions/composition-contract.md (v2.0.0 breaking changes)
- Updated: sidebars.js (added profile-rules, intent-presets)
