# orun-mcp — Implementation Status (as-built)

| ID | Milestone | Status | As-built |
|----|-----------|--------|----------|
| UM0 | Provider composition (`internal/mcpserve`) | ✅ Done | The stdio JSON-RPC 2.0 loop (16 MiB scanner, initialize/ping/tools handling, notification silence, -32700/-32601/-32602 codes) moved verbatim from `internal/workmcp` into `internal/mcpserve`, which now composes `ToolProvider`s: merged `tools/list` in provider order, `tools/call` routed to the owning provider, unknown tool keeps the historical isError-result shape, duplicate tool names across providers fail loudly at serve start. serverInfo → `{name: "orun", version: <binary version>}` (was `orun-work`/`"1"`). `workmcp` keeps `WorkAPI` + the 9 tools and implements `ToolProvider`; `cmd/orun/mcp.go` serves the composite (work provider only until UM1). The WP-3/WP-10 forbidden-name sweep (`mcpserve.ForbiddenNameFragments`: status/pin/lifecycle) now runs over the composed roster in both suites; `./internal/mcpserve` + `./internal/workmcp` wired into `make test-state-redesign` (the PR CI gate) |
| UM1 | Native platform reads + manifest parity | ✅ Done | `internal/platformmcp` (new): the 19 read tools over a `PlatformAPI` seam satisfied by `*remotestate.Client`. Names/titles/descriptions/inputSchemas/annotations are **embedded from the vendored manifest** (`go:embed` of a byte-identical copy of `specs/orun-cloud/vendored/mcp-tool-manifest.json` — MCP9's export, now registered in the vendored `CHECKSUM`), so schema parity holds by construction; `parity_test.go` pins the embed copy, the checksum, and the advertised roster (count/order/names/descriptions/titles/annotations/normalized schemas — scoped to readOnlyHint:true, flips to all 25 in UM2). `internal/remotestate/platform.go`: public-API read methods (`work.go` pattern; org passed per call, never the bound scope; a `PlatformPage {data, meta}` decode keeps cursors), route shapes **pinned against the TS SDK** (orun-cloud `packages/sdk/src` @ f8dcc15): `/v1/auth/profile`, `/v1/organizations` (+ per-org projects/environments, `catalog/entities` + `catalog/docs` + `catalog/doc?digest=` raw-markdown read, org + project `state/runs`/`…/jobs`/`…/logs/{jobId}?fromSeq=`, `audit`, `events`, `effective-access` + `members` + `teams`, `usage/summary`, `quotas/check`, `billing/summary` + `entitlements`, `config/{settings,feature-flags,secrets}` at the discriminated org/project/environment scope, `webhooks/endpoints[/…/delivery-attempts]`) and `/v1/auth/security-events`; no single-catalog-entity route exists (SC0 never shipped) — `catalog_get_entity` emulates over the entity list with exact client-side entityRef matching, following cursors, like the TS tool. Output discipline: one summary line + compact JSON (`meta.cursor` passthrough); 64 KiB cap with the exact `[truncated — N more bytes; refine your query or use fromSeq/cursor]` marker; platform errors keep their code (`<code>: <message> (requestId: …)`). Workspace defaults: serve's resolved scope fills an absent `workspace` (explicit wins) and demotes it from the advertised schemas' `required` (applyWorkspaceDefault semantics). `cmd/orun/mcp.go`: platform provider mounts whenever auth resolves — serve no longer hard-requires a workspace (stderr note when platform-only); work provider mounts as before when a workspace resolves; new `orun mcp tools [--json]` prints the 28-row merged roster with provider + read-only columns. Composed-server test: 28 tools under one initialize, forbidden-name sweep green over all 28; `./internal/platformmcp` wired into `make test-state-redesign` |
| UM2 | Platform writes + rails | ✅ Done | The 6 write tools (project_create, environment_create, flag_set, webhook_create, webhook_delivery_replay, member_invite) unfiltered from the embedded manifest behind `platformmcp.Provider{ReadOnly}` — `Tools()` returns 25 (19 under ReadOnly), and a filtered write is also blocked at execution with an explicit `--read-only` isError. `internal/remotestate/platform.go` write methods, routes **pinned against the TS SDK** @ f8dcc15: `POST …/projects`, `POST …/projects/{prj}/environments`, `POST`/`PATCH <config scope>/config/feature-flags[/{flagId}]`, `POST …/webhooks/endpoints` (org- or project-scoped), `POST …/webhooks/subscriptions`, `POST …/webhooks/delivery-attempts/{attemptId}/replay`, `POST …/invitations`. Header seam: the platform-call path unified on `platformDo(method, path, body, idemKey)` → `platformDoOnce`, which stamps `x-client-surface: mcp` on **every** platform call (reads too) and `Idempotency-Key` on writes (work-plane/state methods untouched — work-MCP provenance is a separate decision, noted in the file header); writes retry under the same key (replay-safe). Idempotency rails: auto key `mcp_<uuid-v4>` (crypto/rand, 40 chars) minted per logical attempt; supplied `idempotencyKey` validated (1-255 printable ASCII, else `validation_failed`) and passed through verbatim. `flag_set` mirrors the TS upsert: scope-exact `ListFeatureFlags` → PATCH the key-matching flag's id, else POST create (body field `flagKey`, per the contracts; ≥1 of enabled/value enforced as `validation_failed`); `webhook_create` fans `events[]` out to subscription creates under deterministic derived keys `<base>:sub<i>` (a retry with the same base key replays identical sub-keys); `member_invite` strips the one-time accept token (every `token` key at any depth — the TS guard, test-pinned). Entitlement gate (`entitlement.go`): first workspace-carrying call (reads + writes) lazily reads `…/billing/entitlements` for `feature.mcp_server` — missing row or failed read grants (fail-open, TS posture), explicit enabled:false denies as `entitlement_required`; 60s TTL per-workspace cache, 64-entry cap. `cmd/orun/mcp.go`: `--read-only` on serve and `mcp tools` (default lists 34 = 9 work + 25 platform; read-only 28 — work tools are mutator-shaped by WP-6 and unaffected, risk U-R3, noted in flag help). Parity test now pins the full 25 (and the 19-tool ReadOnly roster); forbidden-name sweep green over 34; transport tests pin write method/path/body plus both rail headers at a fake HTTP server |
| UM3 | Docs + release | ✅ Done (v2.24.0 released) | `website/docs/cli/orun-mcp.md` rewritten for the unified surface: 34 tools (9 work + 25 platform, grouped by job), client snippets (Claude Code / Cursor / VS Code / generic stdio) pointing at `orun mcp serve`, contextual-mounting + workspace-default semantics, `--read-only` (with the WP-6/U-R3 asymmetry rationale), `orun mcp tools`, the vendored-manifest one-contract note, serverInfo `orun` — the stale 7-tool work-MCP text is gone. `website/docs/release-notes/v2.24.0.md` (v2.22.0's format): unified-MCP headline, write rails, `feature.mcp_server` gate, manifest parity, the `orun-work` → `orun` serverInfo rename called out per risk U-R2, console Connect-page pointer; registered newest-first in `website/sidebars.js`. Verified `provider.yaml` (version stamped by `release-oci.yaml` at tag time) and `install.sh` (resolves the tag from GitHub releases) need no changes for v2.24.0. As released: v2.24.0 published 2026-07-10 via release-oci (workflow_dispatch; the goreleaser job published the GitHub release + binaries on attempt 1, the kiox OCI provider on attempt 2 after a pre-existing flaky concurrency test in session_refresh_test.go — unrelated to UM — passed on rerun). |
| UM4 | Truthful wire metadata + doctor | ✅ Done | **Work-tool wire annotations**: `workmcp.Tools()` now stamps the three hints on all 15 tools via the shared `mcpserve.Annotations(readOnly, destructive, idempotent)` helper — the 9 reads true/false/true; the 6 mutators (task_create, task_comment, task_assign, contract_propose, design_propose, task_regenerate) false/false/**false**: mutator-shaped per WP-6 (nothing deletes or irreversibly overwrites) but truthfully NOT idempotent — unlike the platform writes' per-attempt `Idempotency-Key` (UM2) the work mutators carry no key, and every call appends a coordination-log event (a blind retry duplicates a task/comment/design; task_regenerate mints fresh keys); rationale in the `Tools()` doc comment. `workmcp.ReadOnly()` and the `orun mcp tools` read-only column now derive from the wire annotation (one truth). **Roster guard**: `mcpserve.checkRoster` (run at every serve start) rejects any composed ToolDef missing any of `AnnotationHints` (readOnlyHint/destructiveHint/idempotentHint as bools) — mcpserve-level guard tests per missing hint, plus the composed-server test pins all 40 serialized annotations; note the vendored-manifest parity test covers only the 25 platform tools — the work annotations are this repo's own contract, pinned in `workmcp` `TestWireAnnotations`. **Dynamic counts**: `cmd/orun/mcp.go` derives every number in the `mcp` Long text and the `--read-only` flag help from the live rosters (`countMcpRoster()`; caught immediately: the help still said "9 work" after WH5 made it 15); `orun mcp tools` rows were already roster-derived. **Wrong-backend hint**: `platformmcp.errText` appends ` — hint: the backend URL may not be an Orun Cloud API endpoint (this route exists on api-edge); check 'orun cloud status' and 'orun auth login'` to any platform tool verdict whose `APIError` is `not_found`/404 (both the platform's lowercase code and the legacy backend's status-derived `NOT_FOUND`); forbidden etc. unchanged; work tools untouched — test-pinned both ways. **`orun mcp doctor`** (`cmd/orun/mcp_doctor.go`, `orun cloud check`'s ✓/✗-one-actionable-line style): (a) binary — absolute path via os.Executable + version, plus a `!` warning when `orun` on PATH resolves to a DIFFERENT file (exec.LookPath + EvalSymlinks + os.SameFile — the field report's stale-PATH P0); (b) auth — which source resolves (OIDC/ORUN_TOKEN/session) and session user + expiry, no secret material (test-asserted); an expired access token with a live refresh token is healthy; (c) workspace — serve's exact chain with the winning rung named (--workspace > ORUN_WORKSPACE/ORUN_ORG > intent execution.state > repo link); none is a warning (platform-only mount), not a failure; (d) backend sanity — GET `/v1/auth/profile` (platform) and the work summary (when a workspace resolved) with the resolved credential; 404 on the platform route classifies as "the backend URL is not an Orun Cloud API endpoint" (never over-claimed from the work route); (e) always ends with `claude mcp add orun -- <abs path> mcp serve`. Non-zero exit on any failed check; no `--json` (mirrors `cloud check`, which has none); read-only — GET probes only, no token minting beyond normal resolution. Unit tests cover PATH-mismatch detection, probe classification, the workspace chain, and session-expiry rendering with fakes; the full command is smoke-run (env-dependent), not e2e-tested |
| UM5 | Never fail the handshake | ✅ Done | **Serve never exits before the handshake**: `cmd/orun/mcp_serve.go` (serve moved out of `mcp.go`) replaces the fatal `mcpCloudClient` preamble with `buildMcpMountReport` — backend URL, repo link, workspace chain, and token source resolve best-effort into a `mountReport` (per plane mounted/skipped + reason; auth state ok/absent/expired/error + source + expiry; workspace + winning rung; backend URL or the miss) and `server.Serve` always begins. Outside a git repo / with an unreadable link cache serve now degrades instead of exiting (previously fatal). **Built-in `connection_info` tool** (`internal/mcpserve/conninfo.go`, `ConnectionInfoProvider` — stdlib-only, no remotestate/workmcp/platformmcp imports: it reports a startup snapshot, it never calls): mounted on EVERY serve (the degraded server's only tool; the fully mounted server's 41st = 15 work + 25 platform + 1), read-only true/false/true, returns one JSON object `{auth: {state, source?, expiresAt?}, backendUrl: string\|null, planes: {work/platform: {state: mounted\|skipped, reason?}}, fix?, doctor}` — never token material. **Naming decision**: planned as `auth_status`, but "status" is a `ForbiddenNameFragments` entry (the WP-3/WP-10 work-plane sweep over the merged roster); the sweep was deliberately NOT weakened — the tool was renamed to `connection_info`, and `TestConnectionInfoToolShape` pins that the name avoids every fragment. **Consistent degradation**: absent auth, missing backend URL, and resolution errors all serve `connection_info`-only with the fix line (`run \`orun auth login\`` / `set ORUN_BACKEND_URL or run \`orun auth login\``); a present-but-expired session still mounts both planes (token source resolves; expiry bites per-call) and `connection_info` reports state `expired` — `sessionMountState` (shared with doctor's `describeSessionAuth`, one truth) classifies ok/refresh-live-ok/expired. Per-call: `platformmcp.errText` now appends the login hint (`run 'orun auth login' to refresh`) to `unauthenticated`/`unauthorized`/401 verdicts at the same seam as UM4's wrong-backend hint (platform plane only; work-tool auth failures already surface `cliauth.ErrSessionRevoked`'s own `run \`orun auth login\`` text when the session refresh fails — a raw work-route 401 hint remains platform-only by the UM5 note). **`--verbose`** serve flag prints a multi-line stderr summary (backend URL, auth state/source/detail, token expiry, workspace + source, per-plane mounted/skipped + reason); the default single stderr line now names degraded mode + fix ("degraded: not logged in — run `orun auth login`; only connection_info is available"). Counts stay derived (UM4): `countMcpRoster().server`, help text, and `orun mcp tools` (provider column `server`) all read the live built-in roster. Tests: degraded serve answers initialize + lists exactly `connection_info` (mcpserve + cmd layers), absent-auth output has fix + no secrets, all-ok output has no `fix` key, fully-mounted roster pinned at derived sum AND literal 41, expired/refresh-live/absent session classification (no token material), 401 login hint both spellings (fake API), `--verbose` summary lines, forbidden sweep green with the built-in mounted |
| UM6 | Resources & prompts parity | ✅ Done | **Loop additions** (`internal/mcpserve/server.go`, hand-rolled, same 2024-11-05 loop — U-D1 stays open): `resources/list` (always empty — the TS posture: templates only, concrete enumeration would blow the client's context budget), `resources/templates/list`, `resources/read` (routed to the owning provider by uri; an owned failure is a **-32603 protocol error** carrying the provider's coded message — reads have no isError channel, the TS ResourceReadError posture; a uri matching no template is -32002), `prompts/list`, `prompts/get` (single user-role text message `{messages:[{role:"user",content:{type:"text",text}}]}` + description, the TS SDK shape; unknown prompt and missing required argument are -32602 — required flags enforced by the loop from the advertised defs). **Optional-interface design**: `ResourceProvider` (`ResourceTemplates()`, `ReadResource(ctx, uri) (contents, owned, err)`) and `PromptProvider` (`Prompts()`, `RenderPrompt(name, args) (text, owned)`) are type-asserted off `ToolProvider` at dispatch — workmcp and the conninfo built-in needed zero changes. **Conditional capabilities**: initialize advertises `resources`/`prompts` only when a mounted provider supplies ≥1 (degraded/work-only serves keep `{tools:{}}`, and the methods -32601 exactly as before UM6). **platformmcp implements both** (`resources.go`/`prompts.go`): templates and prompt defs come **verbatim from the embedded manifest's reserved MCP9 stubs** (`resources`/`prompts` sections — provider supplies the stub-less `mimeType: text/markdown`); `catalog://{workspace}/{entityKey}` (entityKey = unpadded base64url(entityRef), `EncodeEntityKey` exported; same list-emulation as catalog_get_entity at limit 100 + doc index + overview body under the 64 KiB marker cap) and `runs://{workspace}/{project}/{runId}` (run + plan-DAG jobs table) render the same markdown sections as the TS resources.ts (identity/relations/provenance/docs/overview; run header/jobs table with the — fallbacks); read errors reuse `errText` (platform code + requestId + UM4/UM5 hints in the message), malformed URIs are `validation_failed`, missing entity `not_found`; resource reads skip the entitlement gate (TS parity — the gate wraps tool calls only) and neither surface filters under `--read-only` (prompts are text templates; a read-only connection resolves every referenced read tool). The 4 prompt texts are ported from prompts.ts verbatim incl. investigate_failed_run's runId/project-conditional branches. **Parity**: `TestResourcesPromptsParity` pins templates + prompt args (incl. required flags) to the vendored manifest sections; the ported TS **drift guard** extracts every snake_case token from all rendered branch texts and asserts each resolves on the composed 41-tool roster (and ≥2 distinct tools per prompt). CLI: `orun mcp tools` unchanged (tools only); the `mcp` help derives the 2-template/4-prompt counts live. Tests: loop-level protocol suite over fakes (conditional capabilities, shapes, -32002/-32602/-32603), provider-level reads over fakeAPI (both templates, fallbacks, byte cap, forbidden/not_found/bad-uri errors), composed-server round-trip (capabilities + templates + a rendered prompt), degraded/full capability asserts at the cmd layer |
