apiVersion: sourceplane.io/v1
kind: ExecutionProfile
metadata:
  name: verify
spec:
  description: Build site and validate Terraform — no plan or apply
  plan:
    scope: changed
  controls:
    cloudflare-pages-terraform:
      build:
        enabled: true
      terraform:
        plan: false
        apply: false
