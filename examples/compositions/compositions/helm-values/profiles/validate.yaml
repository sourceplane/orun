apiVersion: sourceplane.io/v1
kind: ExecutionProfile
metadata:
  name: validate
spec:
  description: Lint and render Helm values against charts
  plan:
    scope: changed
  controls:
    helm-values:
      lint:
        enabled: true
      template:
        enabled: true
