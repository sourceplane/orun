apiVersion: sourceplane.io/v1
kind: ExecutionProfile
metadata:
  name: deploy
spec:
  description: Build and deploy to Cloudflare Pages via Turbo
  plan:
    scope: full
  controls:
    cloudflare-pages-turbo:
      build:
        enabled: true
      deploy:
        enabled: true
